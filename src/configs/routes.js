import PATHS from '../const/paths';

const {
  pathToLogin,
  pathToRecovery,
  pathToRegister,
  pathToProducts,
  pathToErrorPage,
  pathToBackpacks,
  pathToCategory,
  pathToCheckout,
  pathToCart,
  pathToMain,
  pathToThanks,
  pathToPurchases,
  pathToProfile,
  pathToTerms,
  pathToUserAgreement
} = PATHS;

const routes = {
  pathToLogin,
  pathToRecovery,
  pathToRegister,
  pathToProducts,
  pathToErrorPage,
  pathToBackpacks,
  pathToCategory,
  pathToCheckout,
  pathToCart,
  pathToMain,
  pathToThanks,
  pathToPurchases,
  pathToProfile,
  pathToTerms,
  pathToUserAgreement
};
export default routes;
