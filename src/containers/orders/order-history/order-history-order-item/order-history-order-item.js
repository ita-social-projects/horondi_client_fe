import React from 'react';
import OrderItem from '../../order/order-item';

const OrderHistoryOrderItem = ({ item, language }) => (
  <OrderItem name='order-history-order-item' item={item} language={language} />
);

export default OrderHistoryOrderItem;
