import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  mainTitle: {
    textAlign: 'center',
    fontSize: 30,
    fontWeight: 400,
    marginBottom: 0,
    marginTop: 72
  },
  loader: {
    margin: '100px auto'
  }
}));
